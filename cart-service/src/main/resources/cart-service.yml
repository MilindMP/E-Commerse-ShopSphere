spring:
  application:
    name: cart-service

server:
  port: 8006

# Redis configuration (currently disabled)
# spring:
#   data:
#     redis:
#       host: localhost
#       port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/